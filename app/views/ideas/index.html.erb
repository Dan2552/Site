<div class="row">
  <p class="pull-right">
    <%= button "New idea", new_idea_path if can? :manage, Idea %>
  </p>
</div>

<div class="row-fluid span12">
  <% @ideas.each do |idea| %>
      <%= render template: 'ideas/show', locals: {idea: idea} %>
  <% end %>
</div>
</div>
</div>
